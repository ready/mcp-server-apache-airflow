# Airflow MCP Server — credentials template
# Copy this file to .env and fill in the password
# .env is gitignored — never commit it
#
# Get credentials from the data team lead.

AIRFLOW_HOST=https://pidgey.ready-internal.net
AIRFLOW_USERNAME=airflowmcpdev@ready.net
AIRFLOW_PASSWORD=<ask-data-team>
